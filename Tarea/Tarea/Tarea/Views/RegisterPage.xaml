<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Tarea.Views.RegisterPage">
    <ScrollView>
        <StackLayout>

            <Image Style="{StaticResource Picture}"/>

            <Entry Text="{Binding RegisterData.UserName}" Style="{StaticResource EntryUser}" Placeholder="Name"></Entry>

            <Entry Text="{Binding RegisterData.Email}" Style="{StaticResource EntryUser}" Placeholder="Email" Keyboard="Email"></Entry>

            <Entry Text="{Binding RegisterData.Password}" Style="{StaticResource EntryUserPass}" Placeholder="Password"></Entry>

            <Entry Text="{Binding RegisterData.ConfirmPassword}" Style="{StaticResource EntryUserPass}" Placeholder="Confirm Password"></Entry>

            <Button Style="{StaticResource Button}" Command="{Binding SaveRegisterDataCommand}" Text="Register" BackgroundColor="Gray"/>

            <Label Text="{Binding Result, Mode=TwoWay}" Style="{StaticResource LabelResult}"/>


        </StackLayout>
    </ScrollView>
</ContentPage>